//C++ program to check if the person is eligible for voting or not//
#include<iostream>
using namespace std;
int main()
{
    int age;
    cout<<"enter person's age"<<endl;
    cin>>age;
    if(age>=18)
    {
               cout<<"person is eligible for voting"<<endl;
               }
               else
               {
                   cout<<"person is not eligible for voting";
                   }
                   return 0;
                   }

